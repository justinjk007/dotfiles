#!/bin/bash
cp custom-functions.el ~/.emacs.d/
cp custom.el ~/.emacs.d/
cp .gitconfig ~/
cp .aliases ~/
cp .zshrc ~/
cp init.el ~/.emacs.d/
cp -rf snippets ~/.emacs.d/
echo "Finished copying 6 files and 1 folder"
